<UserControl x:Class="Kortros.ParamParser.View.Controls.ParamTargControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Kortros.ParamParser.View.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="200">
    <UserControl.Resources>
        <Style TargetType="Grid" x:Key="ParamInitControlStyle">
            <Style.Triggers>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor,
                    AncestorType={x:Type DataGridRow}}, Path=IsSelected}" Value="True">
                    <Setter Property="Background" Value="Red"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid Background="#FFF1F1F1">
        <StackPanel>
            <TextBlock x:Name="paramName" Text="Имя Параметра"
               FontSize="15" FontWeight="Bold"/>
            <Border BorderThickness="0,0,0,1" BorderBrush="Gray" Margin="5"/>
            <TextBlock x:Name="paramType" Background="#FFE1E1E1" Text="Тип/экземпляр" 
                   Foreground="#FF0063B9"
                   FontStyle="Italic" HorizontalAlignment="Stretch" FontSize="13"/>
            <TextBlock x:Name="paramStorage" Text="Тип данных - строка" FontSize="11"/>
        </StackPanel>
    </Grid>
</UserControl>
